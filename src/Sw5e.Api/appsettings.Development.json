{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "Content": {
    "RootPath": "../../../sw5e-database/content",
    "//SchemaPath": "The submodule, not the sibling checkout beside it. RootPath points at a working copy of the content repository a developer may have edited; the schemas that decide what a write is allowed to be come from the commit this repository pins, so that a local run validates exactly as CI does.",
    "SchemaPath": "../../external/sw5e-database/schemas"
  }
}
